services:
  binance-sync:
    build:
      context: .
      dockerfile: Dockerfile
    image: binance-data-downloader:latest
    container_name: binance-sync
    working_dir: /app
    environment:
      TZ: ${TZ:-Europe/Lisbon}
      START_DATE: ${START_DATE:-2021-12-01}
      SYMBOLS: ${SYMBOLS:-BTCUSDT,ETHUSDT}
    volumes:
      - ${BINANCE_DATA_ROOT:-/media/sam/3TB-WDC/binance-history-data-downloader/data}:/app/data
    command: ["bash", "scripts/run-sync-with-notify.sh"]
    restart: "no"
